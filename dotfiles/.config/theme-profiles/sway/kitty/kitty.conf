# Kitty configuration for sway session
include /home/aesc/.config/kitty/common.conf

# Dynamic wallpaper theme from Noctalia
include /home/aesc/.config/kitty/themes/noctalia.conf

# Optional manual kitten theme override (if selected via kitty +kitten themes)
include current-theme.conf
